[plugin]

name="Hot Reload Verify"
description="E2E check that a new @gdexport property added to a harness class hot-reloads into the editor's inspector property list. Inert unless launched with the 'verify-hot-reload' user arg: godot --headless --editor --path . -- verify-hot-reload"
author="optical002"
version="0.1.0"
script="verify.gd"
